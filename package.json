{
  "name": "trilium-etapi",
  "version": "0.1.0",
  "description": "An easy to use api for Trilium ETAPI.",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "files": ["lib/"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json --outDir dist/ test.ts",
    "debug": "npx @digitak/esrun test.ts"
  },
  "author": "Zerebos",
  "license": "Apache-2.0",
  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@types/node": "^20.5.1",
    "phin": "^3.7.0"
  }
}
